<%= form_with(model: crime_list) do |form| %>
  <% if crime_list.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(crime_list.errors.count, "error") %> prohibited this crime_list from being saved:</h2>

      <ul>
        <% crime_list.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
