<%= form_with(model: conect) do |form| %>
  <% if conect.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(conect.errors.count, "error") %> prohibited this conect from being saved:</h2>

      <ul>
        <% conect.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, class:'form-control' %>
  </div>

  <div class="actions">
    <%= form.submit "Add", class: "btn btn-primary btn-block" %>
  </div>
<% end %>
